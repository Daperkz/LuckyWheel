# ==============================================================================
# LuckyWheel - Java Minecraft Plugin
# Copyright (c) 2026 Daperkz
#
# ci
# ==============================================================================

name: LuckyWheel CI/CD

on:
  push:
    branches-ignore:
      - 'no-ga/**'
    tags:
      - 'v*.*.*'
      - 'v*.*'
  pull_request:
    types: [opened, reopened, synchronize]
    branches-ignore:
      - 'no-ga/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build_and_check:
    name: Build & Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: Set up Java JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'maven'

      - name: Compile Project
        run: make build
        timeout-minutes: 5

      - name: Verify Build Output
        run: |
          if [ ! -f target/LuckyWheel-*.jar ]; then
            echo "::error::Built JAR file not found in target/ directory!"
            exit 1
          fi

      - name: Upload Build Artifact
        if: github.ref_type != 'tag'
        uses: actions/upload-artifact@v7
        with:
          name: LuckyWheel-build-${{ github.sha }}
          path: target/LuckyWheel-*.jar
          retention-days: 7

  release:
    name: Create GitHub Release
    needs: build_and_check
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: Set up Java JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'maven'

      - name: Build Release JAR
        run: make build
        timeout-minutes: 5

      - name: Create GitHub Release & Upload JAR
        uses: softprops/action-gh-release@v3
        with:
          files: target/LuckyWheel-*.jar
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
